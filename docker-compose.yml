services:
  gif-maker:
    build: .
    ports:
      - "3000:5000"
    env_file:
      - .env
    volumes:
      - gif-maker-data:/app/instance
    restart: unless-stopped

volumes:
  gif-maker-data:
